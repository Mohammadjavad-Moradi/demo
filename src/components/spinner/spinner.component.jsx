import React from 'react';

import { SpinnerOverlay, LogoSpinner } from './spinner.styles';

const Spinner = () => (
    <SpinnerOverlay>
        <LogoSpinner />
    </SpinnerOverlay>
);

export default Spinner;